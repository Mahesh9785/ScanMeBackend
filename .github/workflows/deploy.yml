name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Debug SSH connectivity
        run: nc -vz -w 10 ${{ secrets.SERVER_HOST }} ${{ secrets.SERVER_PORT }}

      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          port: ${{ secrets.SERVER_PORT }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /home/feelsoul/apk/ScanMeBackend && \
            git pull && \
            npm install && \
            pm2 restart feelsoul || pm2 start npm --name "feelsoul" -- start
            pm2 save
